import React from "react";

/**
 * PUBLIC_INTERFACE
 * LocalResourcesPage stub for directories and events.
 */
function LocalResourcesPage() {
  return (
    <div className="container" style={{ margin: "3rem auto", maxWidth: 600 }}>
      <h2>Local Resources & Events</h2>
      <p>
        Discover nearby health resources, local events, and directories.<br />
        (Section under construction.)
      </p>
    </div>
  );
}

export default LocalResourcesPage;
